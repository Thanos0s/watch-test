# Production requirements for Render deployment
# Optimized for faster builds and smaller container size

# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.10.0
python-dotenv>=1.0.1
python-multipart>=0.0.9

# AI & LangGraph
langgraph>=0.2.28
groq>=0.11.0

# Database
sqlalchemy>=2.0.30
aiosqlite>=0.20.0

# HTTP & Requests
httpx>=0.27.0
requests>=2.32.0

# Image Processing (lightweight)
pillow>=10.4.0
numpy>=1.26.0
opencv-python-headless>=4.10.0

# OCR (lightweight - pytesseract only for Render)
pytesseract>=0.3.13

# Note: PaddleOCR and PyMuPDF are excluded for Render to reduce build time
# The app will fall back to pytesseract or Groq Vision API
